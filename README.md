# Bank Product Monitoring App

Приложение для мониторинга банковских продуктов с современным графическим интерфейсом на PyQt6, включающее AI-агента для персонализированных рекомендаций.

## 📋 Описание

Bank Product Monitoring App — это десктопное приложение для отслеживания и анализа банковских продуктов (депозиты, кредиты, карты) пяти крупнейших российских банков. Приложение собирает данные напрямую с официальных сайтов банков, нормализует их через AI (GigaChat), хранит в PostgreSQL и предоставляет удобный интерфейс для просмотра данных, построения графиков, анализа и работы с курсами валют ЦБ РФ и данными Московской биржи.

**Ключевые особенности:**
- Прямой парсинг данных с сайтов банков в реальном времени
- AI-агент для персонализированных рекомендаций продуктов
- Локальное хранение данных для обеспечения конфиденциальности
- Интеграция с MOEX для анализа ценных бумаг банков
- Парсинг рейтингов банков с Banki.ru
- Работа с курсами валют ЦБ РФ

## ✨ Основные возможности

- 📊 **Таблица продуктов** — просмотр и фильтрация банковских продуктов с поддержкой множественных фильтров, сортировки и поиска
- 📈 **Графики Финам** — визуализация данных о продуктах (ставки, суммы, тренды) с использованием matplotlib
- 🔍 **Рейтинг Банков** — парсинг и анализ рейтингов банков с Banki.ru по различным показателям (активы, прибыль, капитал, кредитный портфель, вклады) с фильтрацией и сортировкой
- 💱 **Курсы валют** — отображение текущих и исторических курсов валют ЦБ РФ с построением графиков за выбранный период
- 📈 **MOEX** — анализ ценных бумаг банков через API Московской биржи (ISS API) с визуализацией цен закрытия, OHLC и объемов торгов
- 🤖 **AI Чат** — интеллектуальный помощник для персонализированных рекомендаций банковских продуктов на основе GigaChat API
- ⚙️ **Настройки** — настройка приложения, параметров парсинга, фильтров по умолчанию и автообновления данных
- 📝 **Логи** — просмотр логов работы приложения с фильтрацией по уровню (INFO, WARNING, ERROR)
- 💾 **Экспорт данных** — экспорт данных в форматы CSV, JSON, XLSX

## 🏦 Поддерживаемые банки

Приложение собирает данные со следующих банков:

- **Сбербанк** (Selenium) — дебетовые карты, кредитные карты, кредитные продукты
- **ВТБ** (Playwright) — дебетовые карты, кредитные карты, кредитные продукты
- **Альфа-Банк** (Playwright) — дебетовые карты, кредитные карты, кредитные продукты
- **Т-Банк** (бывший Тинькофф) (Playwright) — дебетовые карты, кредитные карты, кредитные продукты
- **Газпромбанк** (Playwright) — дебетовые карты, кредитные карты, кредитные продукты

**Дополнительные источники данных:**
- **Banki.ru** — рейтинги банков по различным показателям (активы нетто, чистая прибыль, капитал, кредитный портфель, вклады физических лиц) с возможностью выбора дат и регионов
- **MOEX ISS API** — данные о ценных бумагах банков (акции, облигации) с историей торгов
- **ЦБ РФ API** — текущие и исторические курсы валют (через cbr-xml-daily.ru)

**Особенность парсинга Сбербанка:** Сайт Сбербанка требует установки корневых сертификатов Минцифры для доступа через SSL/TLS-соединение. Для парсинга Сбербанка используется Selenium, который работает с реальным установленным браузером и имеет доступ к системным сертификатам Windows.

## 🛠 Технологии

- **Python 3.13+** — язык программирования
- **PyQt6** — графический интерфейс пользователя
- **qasync** — интеграция asyncio с Qt event loop
- **SQLAlchemy 2.0+** — ORM для работы с PostgreSQL
- **PostgreSQL 12+** — реляционная база данных для хранения продуктов и курсов валют
- **Playwright** — автоматизация браузера для парсинга (большинство банков)
  - `playwright-stealth` — обход антибот-защиты
- **Selenium 4.15+** — автоматизация браузера для парсинга Сбербанка (требует установленные сертификаты Минцифры)
  - `webdriver-manager` — автоматическое управление драйверами
- **GigaChat API** — AI для нормализации данных и персонализированных рекомендаций
- **httpx** — современный асинхронный HTTP-клиент для работы с API (MOEX, ЦБ РФ)
- **aiohttp** — асинхронные HTTP-запросы
- **BeautifulSoup4** — парсинг HTML (для некоторых парсеров)
- **lxml** — быстрый XML/HTML парсер
- **matplotlib** — построение графиков и визуализация данных
- **pandas** — обработка табличных данных (MOEX)
- **numpy** — численные вычисления
- **openpyxl** — экспорт данных в Excel (XLSX)
- **fake-useragent** — генерация случайных User-Agent для парсинга
- **python-dotenv** — загрузка переменных окружения из .env
- **uv** — быстрый менеджер зависимостей и пакетов Python

## 📦 Установка

### Требования

- **Python 3.13** или выше
- **[uv](https://github.com/astral-sh/uv)** — быстрый менеджер пакетов Python
- **PostgreSQL 12+** (для хранения данных)
- **Chrome или Firefox** (для парсинга Сбербанка через Selenium)
- **Windows 10/11** (для парсинга Сбербанка, так как требуется установка сертификатов Минцифры в систему)
- **Интернет-соединение** (для парсинга данных и работы с API)

### Установка uv

```bash
# Windows (PowerShell)
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"

# Linux/macOS
curl -LsSf https://astral.sh/uv/install.sh | sh
```

### Клонирование и установка зависимостей

```bash
# Клонируйте репозиторий
git clone https://github.com/yourusername/bank-product-monitoring-app.git
cd bank-product-monitoring-app

# Установите зависимости
uv sync

# Установите браузеры для Playwright
uv run playwright install chromium firefox webkit
```

### Настройка базы данных PostgreSQL

1. **Создайте базу данных PostgreSQL:**
   ```bash
   # Подключитесь к PostgreSQL
   psql -U postgres

   # Создайте базу данных (или используйте существующую)
   CREATE DATABASE bank_monitor;

   # Или используйте существующую базу данных, указав её имя в .env

   # Выйдите из psql
   \q
   ```

2. **Настройте переменные окружения:**

   Скопируйте файл `env.example` в `.env`:
   ```bash
   cp env.example .env
   ```

   Отредактируйте `.env` и укажите параметры подключения к БД:
   ```env
   DB_HOST=localhost
   DB_PORT=5432
   DB_NAME=localhost
   DB_USER=postgres
   DB_PASSWORD=postgres

   # Настройки парсинга (опционально)
   PARSING_TIMEOUT=30.0
   PARSING_RETRIES=3
   PARSING_HEADLESS=true

   # Настройки автообновления (опционально, в минутах)
   AUTO_REFRESH_INTERVAL_MINUTES=360

   # GigaChat API (опционально, но рекомендуется для нормализации данных и AI-агента)
   GIGACHAT_AUTH_KEY=your_gigachat_auth_key_here
   ```

   **Примечание:** Для получения ключа GigaChat API зарегистрируйтесь на [https://developers.sber.ru/](https://developers.sber.ru/)

3. **Установка сертификатов Минцифры (для парсинга Сбербанка):**

   Для парсинга сайта Сбербанка необходимо установить корневые сертификаты Минцифры:
   - Скачайте сертификаты с официального сайта Минцифры: [https://www.gosuslugi.ru/crt](https://www.gosuslugi.ru/crt)
   - Установите их в систему Windows (двойной клик по файлу .cer → "Установить сертификат" → "Поместить все сертификаты в следующее хранилище" → "Доверенные корневые центры сертификации")
   - Перезапустите браузер Chrome/Firefox (если используете Selenium)
   - **Важно**: Без установленных сертификатов парсинг Сбербанка будет невозможен из-за SSL/TLS ошибок

4. **Таблицы создадутся автоматически** при первом запуске приложения.

## 🚀 Запуск

```bash
# Запуск приложения
uv run python src/app.py
```

Или через uv напрямую:

```bash
uv run src/app.py
```

### Поведение при запуске

- **При старте приложения**: Данные автоматически загружаются из базы данных PostgreSQL (если БД доступна)
- **Парсинг данных**: Запускается только при нажатии на кнопку **"Обновить"** (refreshButton) в главном окне или через автоматическое обновление (если включено в настройках)
- **Обновление данных**: После парсинга данные сохраняются в БД, неактуальные записи помечаются как неактивные (`is_active = False`)
- **Очистка БД**: Записи, помеченные как неактивные более 7 дней, автоматически удаляются при обновлении
- **Нормализация через AI**: Опционально выполняется для кредитных продуктов и карт (требует `GIGACHAT_AUTH_KEY`)
- **Парсинг рейтингов**: Запускается отдельно через виджет "Рейтинг Банков" с выбором параметров (показатель, даты, регион)
- **Курсы валют**: Автоматически загружаются при открытии вкладки "Курсы валют"
- **MOEX данные**: Парсятся по запросу через виджет "MOEX"

## 📁 Структура проекта

```
bank-product-monitoring-app/
├── pyproject.toml              # Конфигурация проекта и зависимости
├── uv.lock                     # Блокировка версий зависимостей
├── env.example                 # Пример файла конфигурации (.env)
├── .env                        # Файл конфигурации (создается из env.example)
├── src/
│   ├── app.py                  # Инициализация и запуск приложения
│   ├── core/                   # Ядро приложения
│   │   ├── models.py           # Модели данных (BankProduct, Filters, LogEntry и т.д.)
│   │   ├── database.py         # Модели SQLAlchemy для PostgreSQL
│   │   ├── config.py           # Конфигурация (переменные окружения)
│   │   ├── parsers/            # Парсеры для различных банков
│   │   │   ├── base.py         # Базовый класс BaseParser
│   │   │   ├── banki_ratings.py           # Парсер рейтингов Banki.ru
│   │   │   ├── moex_securities.py         # Парсер данных MOEX
│   │   │   ├── sberbank_debit_card.py     # Сбербанк: дебетовые карты (Selenium)
│   │   │   ├── sberbank_credit_card.py    # Сбербанк: кредитные карты (Selenium)
│   │   │   ├── sberbank_credit_products.py # Сбербанк: кредитные продукты (Selenium)
│   │   │   ├── vtb_debit_card.py          # ВТБ: дебетовые карты (Playwright)
│   │   │   ├── vtb_credit_card.py         # ВТБ: кредитные карты (Playwright)
│   │   │   ├── vtb_credit_products.py     # ВТБ: кредитные продукты (Playwright)
│   │   │   ├── alpha_debit_card.py        # Альфа-Банк: дебетовые карты
│   │   │   ├── alpha_credit_card.py       # Альфа-Банк: кредитные карты
│   │   │   ├── alpha_credit_products.py   # Альфа-Банк: кредитные продукты
│   │   │   ├── tinkoff_debit_card.py      # Т-Банк: дебетовые карты
│   │   │   ├── tinkoff_credit_card.py     # Т-Банк: кредитные карты
│   │   │   ├── tinkoff_credit_products.py # Т-Банк: кредитные продукты
│   │   │   ├── gazprombank_debit_card.py  # Газпромбанк: дебетовые карты
│   │   │   ├── gazprombank_credit_card.py # Газпромбанк: кредитные карты
│   │   │   └── gazprombank_credit_products.py # Газпромбанк: кредитные продукты
│   │   ├── normalizers/        # Нормализаторы данных
│   │   │   └── gigachat.py     # Нормализация через GigaChat API
│   │   └── services/           # Бизнес-логика
│   │       ├── data_service.py             # Сервис для работы с данными
│   │       ├── database_service.py         # Сервис для работы с БД
│   │       ├── parsing_service.py          # Сервис для парсинга
│   │       ├── chat_service.py             # Сервис AI-агента (GigaChat)
│   │       ├── currency_rates_service.py   # Сервис для работы с курсами валют
│   │       ├── export_service.py           # Сервис для экспорта данных
│   │       └── logger_service.py           # Сервис для логирования
│   ├── ui/                     # Пользовательский интерфейс
│   │   ├── main_window.py      # Главное окно
│   │   ├── ui_main_window.py   # Сгенерированный UI код
│   │   ├── components/         # Переиспользуемые компоненты
│   │   │   └── stat_card.py    # Компонент карточки статистики
│   │   └── widgets/            # Виджеты приложения
│   │       ├── table_widget.py              # Таблица продуктов с фильтрацией и сортировкой
│   │       ├── charts_widget.py             # Графики продуктов (matplotlib)
│   │       ├── banki_ratings_widget.py      # Виджет рейтингов Banki.ru с парсингом и фильтрацией
│   │       ├── moex_charts_widget.py        # Виджет графиков MOEX (цены, OHLC, объемы)
│   │       ├── currency_widget.py           # Виджет текущих курсов валют ЦБ РФ
│   │       ├── currency_history_widget.py   # Виджет истории курсов валют с графиками
│   │       ├── currency_tab_widget.py       # Вкладка курсов валют (объединяет currency_widget и currency_history_widget)
│   │       ├── settings_widget.py           # Настройки приложения
│   │       └── logs_widget.py               # Логи с фильтрацией
│   └── resources/              # Ресурсы (UI файлы, иконки)
│       ├── ui/
│       │   └── main_window.ui  # UI файл для QtDesigner
│       └── styles.qrc          # Ресурсный файл стилей
└── test_parsers.py             # Тесты парсеров
```

## 🔧 Разработка

### Редактирование UI

UI приложения создан с помощью QtDesigner. Файл интерфейса находится в `src/resources/ui/main_window.ui`.

После редактирования UI файла необходимо сгенерировать Python код:

```bash
uv run pyuic6 src/resources/ui/main_window.ui -o src/ui/ui_main_window.py
```

### Архитектура

Приложение использует модульную трехслойную архитектуру:

- **Слой представления (UI):** PyQt6 виджеты и компоненты
  - `ui/main_window.py` — главное окно
  - `ui/widgets/` — специализированные виджеты
  - `ui/components/` — переиспользуемые компоненты

- **Слой бизнес-логики (Services):** Сервисы для обработки данных
  - `core/services/parsing_service.py` — координация парсинга
  - `core/services/data_service.py` — работа с данными
  - `core/services/database_service.py` — взаимодействие с БД
  - `core/services/chat_service.py` — AI-агент для рекомендаций
  - `core/services/currency_rates_service.py` — курсы валют

- **Слой доступа к данным (Database):** SQLAlchemy модели и менеджер БД
  - `core/database.py` — модели БД и DatabaseManager
  - `core/models.py` — модели данных приложения

**Парсеры:**
- `core/parsers/base.py` — базовый абстрактный класс BaseParser
- Специализированные парсеры для каждого банка и типа продукта
- Используют Playwright (большинство банков) или Selenium (Сбербанк)

**Нормализация данных:**
- `core/normalizers/gigachat.py` — нормализация через GigaChat API
- Используется для приведения неструктурированных данных к единому формату

### Паттерны проектирования

- **Template Method Pattern:** `BaseParser` определяет алгоритм парсинга, наследники реализуют детали
- **Service Layer Pattern:** Бизнес-логика инкапсулирована в сервисах
- **Repository Pattern:** `DatabaseService` инкапсулирует логику работы с БД
- **Dependency Injection:** Сервисы получают зависимости через конструктор

## 📝 Модели данных

### BankProduct

Основная модель банковского продукта:

- `id` — уникальный идентификатор (UUID)
- `bank` — название банка
- `product` — название продукта
- `category` — категория (`DEPOSIT`, `CREDIT`, `DEBIT_CARD`, `CREDIT_CARD`)
- `rate_min/rate_max` — минимальная/максимальная ставка (%)
- `amount_min/amount_max` — минимальная/максимальная сумма
- `currency` — валюта (`RUB`, `USD`, `EUR`, `CNY`)
- `confidence` — уровень уверенности в данных (`high`, `medium`, `low`)
- `grace_period` — льготный период (для кредитных карт)
- `cashback` — кешбэк (для карт)
- `commission` — комиссия
- `term` — срок размещения/кредитования
- `collected_at` — дата сбора данных
- `created_at` — дата создания записи
- `updated_at` — дата последнего обновления
- `is_active` — флаг актуальности (True/False)
- `unique_key` — уникальный ключ для предотвращения дубликатов (`{bank}|{product}|{category}`)

### Filters

Модель фильтров для поиска:

- `bank` — фильтр по банку
- `category` — фильтр по категории
- `currency` — фильтр по валюте
- `date_from/date_to` — диапазон дат
- `search_query` — текстовый поиск
- `rate_range` — диапазон ставок
- `amount_range` — диапазон сумм

### CurrencyRate

Модель курса валют ЦБ РФ:

- `id` — уникальный идентификатор
- `code` — код валюты (USD, EUR, CNY и т.д.)
- `name` — название валюты
- `nominal` — номинал
- `value` — курс валюты
- `previous` — предыдущий курс
- `rate_date` — дата курса
- `created_at` — дата создания записи
- `updated_at` — дата последнего обновления

## 🎯 Основные функции

### Парсинг данных

- Автоматический парсинг с сайтов 5 крупнейших банков (Сбербанк, ВТБ, Альфа-Банк, Т-Банк, Газпромбанк)
- Поддержка динамического контента через Playwright (большинство банков) и Selenium (Сбербанк)
- Обход антибот-защиты через playwright-stealth и fake-useragent
- Автоматическое извлечение данных о продуктах (ставки, суммы, сроки, условия, кешбэк, льготный период)
- Обработка ошибок с автоматическими повторными попытками (настраивается через PARSING_RETRIES)
- Парсинг рейтингов банков с Banki.ru с поддержкой пагинации и различных показателей
- Парсинг данных MOEX через ISS API

### Нормализация через AI

- Автоматическая нормализация неструктурированных данных через GigaChat API
- Приведение данных к единому формату JSON Schema
- Извлечение числовых значений из текста
- Распознавание единиц измерения и преобразование в стандартный формат

### AI-агент для рекомендаций

- Диалоговый интерфейс для взаимодействия с AI
- Персонализированные рекомендации на основе запросов пользователя
- Сравнение продуктов различных банков с конкретными цифрами
- Объяснение финансовых терминов простым языком
- Использование актуальных данных из БД для контекстных рекомендаций

### Работа с данными

- Фильтрация и сортировка продуктов по различным параметрам
- Построение графиков и визуализация трендов
- Экспорт данных в CSV, JSON, XLSX
- Анализ рейтингов банков с Banki.ru
- Работа с курсами валют ЦБ РФ (текущие и исторические)
- Интеграция с MOEX для анализа ценных бумаг банков

## 🔒 Безопасность и конфиденциальность

- **Локальное хранение данных:** Все данные хранятся локально в PostgreSQL
- **Отсутствие передачи данных:** Данные о запросах пользователя не передаются на внешние серверы
- **Конфиденциальность:** Полный контроль пользователя над своими данными
- **Безопасная работа с API:** Использование HTTPS для всех внешних API (GigaChat, MOEX, ЦБ РФ)
- **Учетные данные:** Хранение паролей и API ключей в `.env` файле (не коммитится в Git)

## 📊 Преимущества по сравнению с существующими решениями

Разработанное приложение решает следующие проблемы, которые не решают агрегаторы типа sravni.ru:

1. **AI-агент для персонализированных рекомендаций** — анализ конкретных потребностей пользователя в диалоговом режиме
2. **Актуальность данных** — прямой парсинг с сайтов банков в реальном времени
3. **Конфиденциальность** — локальное хранение данных без передачи на внешние серверы
4. **Расширенная аналитика** — интеграция с MOEX и Banki.ru, графики, история изменений
5. **Работа офлайн** — возможность работы с ранее загруженными данными

## 🐛 Известные проблемы и ограничения

- **Парсинг Сбербанка** требует установки корневых сертификатов Минцифры в систему Windows
- **Изменение структуры сайтов** банков может требовать обновления парсеров
- **AI-агент** требует API ключ GigaChat (бесплатный, но с ограничениями по количеству запросов)
- **Парсинг Banki.ru** может занимать время при большом количестве страниц (300+ банков)
- **MOEX API** имеет ограничения по частоте запросов
- **Курсы валют ЦБ РФ** доступны только за рабочие дни (без выходных)

## 📄 Лицензия

Проект находится в разработке. Лицензия будет указана позже.

## 🤝 Вклад в проект

Проект находится в активной разработке. Если вы хотите внести вклад:
1. Создайте форк репозитория
2. Внесите изменения в отдельной ветке
3. Создайте Pull Request с описанием изменений

## 📚 Дополнительная информация

### Особенности реализации

- **Асинхронное программирование**: Использование `async/await` и `qasync` для неблокирующих операций
- **Модульная архитектура**: Разделение на слои (UI, Services, Database) для упрощения поддержки
- **Уникальные ключи**: Предотвращение дубликатов через `unique_key` (`{bank}|{product}|{category}`)
- **Управление актуальностью**: Автоматическое помечание старых данных как неактивных и удаление через 7 дней
- **Обработка ошибок**: Retry-логика для парсинга, graceful degradation при недоступности сервисов

---

**Примечание:** Это приложение находится в активной разработке. Некоторые функции могут быть улучшены или дополнены в будущих версиях.
